import 'package:flutter/material.dart';

class CampHome extends StatelessWidget {
  const CampHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Welcome Camp'),
    );
  }
}
